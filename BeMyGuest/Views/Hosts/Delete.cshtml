@{
    Layout ="_Layout";
}

@model BeMyGuest.Models.Host

<h2>Delete Host</h2>
<hr />
<h4>Are you sure you want to delete this host?</h4>

<div class="card mx-auto" style="width: 36rem;">
    <div class="card-body">
        <div class="form-group row">
            @Html.DisplayNameFor(model => model.HostName): @Html.DisplayFor(model => model.HostName)
            @using (Html.BeginForm())
            {
                <input type="submit" value="Delete" class="btn btn warning" />
            }
        </div>
    </div>
</div>

<h3>@Html.ActionLink("Back to Host Details", "Details", new {id = Model.HostId}) || @Html.ActionLink("Back to Hosts", "Index")</h3>